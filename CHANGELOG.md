# Changelog

## Search-to-content business operations — 2026-09-07

- Added project-scoped manual measurements, atomic bounded imports, source-aware deduplication, conflict rejection, safe integer validation and unknown-aware totals. Additive migration `b82e4c65f901`; projects with measurements cannot be deleted through the catalog endpoint.
- Added Search & Leads navigation with TSV preview/import, real inserted/duplicate counts, source/channel/date filters and explicit attribution limitations.
- Imported queries can create/reuse a Question and prepare an unsaved Opportunity with provenance. Human value, fit and priority are required; six-part content briefs feed existing Action acceptance fields without publication.
- Browser acceptance caught and fixed asynchronous relation-option loading losing preselected questions in the create-action form; operational owner/deadline requirements are now shown before submission.
- Backend regression: 165 passed, 1 skipped. Frontend typecheck and production build passed. Isolated browser/API acceptance verified null versus zero, duplicate import, query-to-opportunity and saved action/question/brief linkage. No customer database, live model or CMS writes; no claim of search or commercial lift.

## Reference-led SEO / GEO improvements — 2026-09-07

- Added immutable static canonical/hreflang observations with location evidence, relative/base URL resolution, duplicate/invalid declarations and conservative cross-page/homepage review findings. No target-link requests or schema migration.
- Agent planning now receives up to 40 findings per selected snapshot plus business-content guidance for customer conditions, routes, cost breakdowns, verified cases and measurement. Reference companies never become project identity or approved facts.
- Page operations labels technical changes requiring website code and retains finding-to-action flow. Existing body publication does not claim to fix metadata or templates.
- Verification: full regression 134 passed / 1 skipped (OpenSSL unavailable); final diagnostic suite 6 passed and Agent payload test 1 passed independently. Typecheck and production build passed. Read-only reference FAQ probe reproduced canonical-other-page and hreflang-homepage findings; no reference-site or formal project writes. Live model content quality remains untested.

## GEO evidence-driven business decisions — 2026-09-06

- Fixed the backend causal ordering bug: approved baseline creation/collection/durable sync/rules analysis now finishes after plan materialization and **before** strategy refinement and draft model HTTP, rather than first sampling after draft approval. Existing Razormind, original content/action models and approval-bound WordPress publishing are reused; no frontend changes or database migration.
- Added bounded immutable same-project Answer/QuestionVersion/Batch/platform/region/source-condition evidence, pinned Analysis IDs/versions and deduplicated AnalysisRun snapshots to planning; reviewed sourced research/rules retain applicability limits and exclude stale/refuted/changed sources.
- Added one budgeted durable real-model strategy call. Every judgment needs exact original evidence; whole-answer brand absence, approved Fact/time-bound mismatch, scope and quote guards reject fabricated or expanded evidence. Strategy semantics are independent from rules Analysis, whose recommendation/factual unknown values are not overwritten.
- Draft generation actually consumes the refined strategy, freezing strategy→change→acceptance and provenance into ContentVersion.generation. Existing summary/expected_change/acceptance_method explain concrete evidence; no-AI routes explicitly remain website-content hypotheses. Unsupported strategies block; expanded scope returns to a new human plan review.
- Legacy unsubmitted old drafts require new evidence-driven drafts/review; existing external publication jobs are never resent. Durable model budgets, unknown-call no-retry, fact/question drift, exact title/body/URL/hash and WordPress preflight gates remain enforced.
- Finish exposes actual per-platform hit/denominator, semantic unknown, comparability and small-sample caveats through existing outcome reasons. Empty groups are skipped and zero-known denominators stay unknown. Action done means content execution completed, not proven GEO improvement.
- Controlled real HTTP A/B acceptance used identical website/facts/goal with only baseline answer text changed: brand absence versus service-scope misstatement yielded different cited strategies, instructions, acceptance and bodies. Each completed baseline before draft HTTP, then exactly one explicitly approved WordPress POST and one retest (two collection tasks total). No-source hypothesis and final deduplicated payload were independently re-exercised; these are protocol fixtures, not live commercial-model quality or GEO-lift evidence. See `docs/business-execution-proof.md`.
- Final integrated regression on the deduplicated strategy implementation: `python -m pytest tests -q` → **130 passed in 170.11s** (existing 118 plus 12 business-risk cases). Final isolated real HTTP smoke also verified evidence deduplication and readable unknown/small-sample results; owned 8001/18771 services and smoke database/sidecars were removed. Production restoration is delegated to the independent HTTP proof owner after final readiness.

## Agent-first backend — 2026-09-06

- Added a bounded real-model Agent executor: goal → automatic same-origin discovery/HTTP evidence → actionable plan review → existing Question/Opportunity/Action/Content materialization → immutable model drafts and actual feedback revisions → explicit publish authorization → original WordPress POST/read-only verification → completed Action and maintenance scheduling. No second task/content system or fake model fallback.
- Added additive migration `a71d9b33e204` for AgentRun/AgentStep/AgentApproval; durable unique checkpoints, conditional database claims, 300-second leases and heartbeat survive browser closure/restarts. Model calls without a durable result are never automatically repeated; unknown publication uses original unique jobs and only GET recovery.
- Separated approve-only from exact body/version/page/URL/title authorization. Frozen WordPress resource body/modified baselines reject stale drafts before POST; frozen model facts/target/source are rechecked after HTTP returns. Active HTML and dangerous URLs are locally rejected before review. Old/parallel approvals and cancellation cannot advance future writes; already-started side effects are not claimed undone.
- Candidate facts remain reviewed exact snapshot quotes, reuse only currently valid same-project exact claim/source matches, and never overwrite human facts. Model business classifications/scores include a reviewed subjective basis, not fixed midpoint defaults or measured demand.
- Added capability-gated original-source baseline/retest collection with durable advancing read-only polling: normal pending states continue automatically, unknown submissions never retrigger. Optional approved due maintenance creates a fresh linked evidence/plan-review run without auto-publication. Without sampling, results explicitly state page optimization completed and effects awaiting observation.
- Worker scheduler/claim faults no longer disappear into a blanket exception: only known transient database contention waits; unexpected errors stop the worker and expose a safe unready reason, without leaking exception values or secrets.
- Actual isolated HTTP smoke completed revised draft → one controlled WP POST → public full-body verified; actual pending→completed baseline/retest ordering and authorized-versus-disabled due-maintenance scenarios passed. These peers are clearly labeled protocol fixtures, not real paid-model/production-account or GEO-lift acceptance. API and operator documentation are in `docs/agent-api.md` and `docs/agent-operations.md`.
- Final integrated backend regression: `python -m pytest tests -q --tb=short` → **118 passed in 115.53s** (original 106 plus 12 Agent risk regressions). PostgreSQL `alembic upgrade head --sql` compiled the new head offline with exit 0; no PostgreSQL runtime claim. Isolated smoke services/database sidecars were removed after frontend use; official database restoration remains the frontend coordinator's responsibility.

## Agent-first frontend — 2026-09-06

- Replaced the primary manual-operation navigation with Studio, Review and Results; preserved all seven original areas behind progressive advanced-tool navigation.
- Added durable backend-run polling with abort/remount isolation, genuine capability/block/error states, one-goal onboarding, bounded scope choices, capability-gated sampling and opt-in continuous maintenance. No fake thinking, progress percentages or client-side Agent executor.
- Added full visible-body before/after and readable sources, plan candidate-fact review, immutable feedback revisions, locally remembered audit identity, direct approvals and explicit publication side-effect authorization bound to exact title/body/target. Approve-only never writes externally; missing WordPress baseline blocks publishing.
- Real browser flow on isolated API 8001 completed run 4: goal → plan 52 → draft 57 → feedback → revised draft 61 → explicit authorization → publication job 2 automatically verified with persisted maintenance date. Refresh preserved the run; no fetch/generate/publish/sync/verify tool buttons were required. Controlled protocol peers are not live billed model or production WordPress acceptance.
- Actual 1440px and 390px studio/review/results screenshots were reviewed and moved to system temporary storage, not retained as repository PNGs; mobile document width matched 390px. All seven advanced areas, project-switch isolation and actual unconfigured-model UI were exercised. Closing the tab after run 10 returned queued still led to awaiting_review with completed model/fetch steps. Standalone frontend typecheck passed; protocol proof JSONs remain in the ignored `artifacts/agent-studio` directory.
- Final default `.next` production build passed (Next 15.5.25; homepage 33.1 kB / first-load 136 kB), followed by standalone `tsc --noEmit` after isolated-output cleanup (exit 0, 2.82s). Restored the original persistent `scripts/start-local.ps1 -Production` service on 3000/8000 without a custom output directory.
- Preserved a stopped-write SQLite backup at `backups/geo-before-agent-upgrade-20260906-020102.db`; startup migrated the formal database from `9c371d04b221` to `a71d9b33e204`. Health/database ok, Agent worker running, proxy projects `[]`, integrity ok and no foreign-key errors; both projects and Agent runs remain zero. Formal empty-homepage 1440/390px browser review passed, with 390px document width. Temporary `.next-agent` / `.next-production` outputs and smoke services/databases were removed; production remains running.

## 2.0.0 — 2026-09-06

### GEO optimization operations backend

- Shifted the primary workflow to customer-question opportunities, evidence or explicit hypotheses, existing-page improvements and real operating plans. Extended the existing Action with owner/due/acceptance, opportunity/page/finding relationships and blocked/review states; summary exposes actual weekly/today/review/ready-to-publish/awaiting-verification/maintenance queues.
- Added immutable HTTP and clearly labeled manual HTML page snapshots, located diagnostic findings, static visible-body extraction, distinct search/training robots observations, and snapshot/fact/question-version-backed manual or real model drafts. No fabricated search volume, ranking scores, mandatory llms.txt or automated publishing.
- Added pinned-IP DNS/TLS fetching, per-hop SSRF checks, private/metadata/credential rejection, exact server-controlled internal allowlists and bounded bodies/deadlines/redirects. Authenticated WordPress requests never follow redirects; remote Application Password authentication requires HTTPS.
- Added real WordPress REST updates for configured existing pages/posts only: exact resource ID/link preflight, immutable approved-version and fact gates, durable unique version/page publication reservation, single explicit write, uncertain submission without blind retries, read-only recovery, and complete approved visible-body verification with immutable hashes/times. Kept external publication records semantically separate. Removed the old unreviewed `/content-versions/{id}/publish` endpoint and the unproven new Razormind website-publisher branch; original Razormind answer collection is unchanged.
- Added immutable external research-source HTTP snapshots, exact source/answer quotation offsets, reviewed research history and evidence-gated reusable rules. Experiments freeze baseline/retest answers, AnalysisRun bases, control groups, three known-denominator metric intervals and publication timing; supported/refuted require matching evidence and chronology. Low sample counts warn rather than pretend 30 is a scientific validity threshold.
- Fixed new manual/automatic answer ingestion to persist server-receipt observation time when no explicit valid timestamp is provided, with raw provenance; no historical rows are backfilled. Baseline must precede the intervention window and retest follow first successful verification; absent or reversed historical timing permits only inconclusive conclusions.
- Added additive migrations `3712685b1ab1` and `9c371d04b221`; initial migration and official user data are preserved. Historical `published_at` does not imply approval or verified publication. Updated API/architecture/operations documentation and server-only WordPress environment examples.

### Operations backend verification

- First integrated run: `python -m pytest tests -q --tb=short` → **99 passed in 55.05s**. Final run after observation-default and intervention-order fixes: **106 passed in 51.44s**, including the original 22 regressions, new receipt-time regression, 33 website tests, 39 research tests and 11 publication/migration tests.
- Final real uvicorn HTTP smoke from an empty isolated SQLite database passed opportunity → page HTTP snapshot/finding → existing Action → actual baseline receipt → immutable draft → review → controlled WordPress-protocol HTTP POST → full visible-body verified → due maintenance → actual later retest receipt → comparable analysis → supported observational experiment → externally fetched research source → reviewed rule. Exactly one peer write occurred; default `server_received` timestamps were exercised, with baseline before job creation and retest after first successful verification.
- Controlled peers are protocol fixtures, not a production WordPress deployment or real AI-platform improvement evidence. No production account writes, paid model calls, cookie inspection or official database reseeding were performed.
- Legacy-schema upgrade regression retained user project/aliases, Action state, original content and historical publication time; new review state remained pending and SQLite foreign-key checks were clean.
- PostgreSQL offline Alembic compilation generated 501 SQL lines / 15,971 bytes with exit 0; no PostgreSQL connection or runtime acceptance is claimed. The known unavailable Docker daemon was not re-probed.

### GEO operations frontend

- Reoriented the primary navigation around the operations workbench, opportunities/plans, page optimization, content/review/publication and research/experiments. Preserved facts, questions, original answers, analysis, diagnosis and matched retests under supporting evidence and acceptance areas.
- Added real task/review/publication/verification/maintenance queues, snapshot-backed page findings, opportunity-to-task and finding-to-task flows, approved-version publication with explicit authorization, full-body verification history, external research snapshots and evidence-gated experiments/rules. WordPress credentials remain server-only; old unreviewed publication controls were removed.
- Preserved the paper/ink editorial design and original SVG, compacted actual queue counts into a responsive strip, retained state during same-project refreshes, and added explicit observation-time entry for experiment evidence. No fake score, example project or automated publication is introduced.
- Real browser interaction on an isolated database exercised page registration/fetch/manual snapshots, opportunity and finding tasks, sourced drafts, approval, one controlled WordPress-protocol update, verification, maintenance, research-source capture, baseline/retest analysis, an inconclusive experiment and a reviewed rule. The workbench `kind=verify` link opened Content & Publication and reached actual verification. This demonstrates the workflow, not a real GEO uplift or deployed WordPress account integration.
- Frontend typecheck and production build passed. Desktop 1440px and mobile 390px browser review passed with no horizontal overflow; final formal empty homepage keeps the operations positioning. The official SQLite database was backed up with SQLite's consistent backup API, migrated without seeding, and restored through the production launcher.

## 1.0.0 — 2026-09-05

### GEO backend

- Added a persistent FastAPI / SQLAlchemy modular monolith with Alembic initial schema migration and PostgreSQL configuration; SQLite supports complete service-free local operation. No seeded demo projects or fabricated dashboard data.
- Added projects (brand aliases, website and region), competitors, sourced time-bounded facts, immutable question versions, archived historical references, and configurable source parameter/result mappings.
- Added immutable collection snapshots and raw answers, explicit manual import, durable Razormind trigger/sync jobs, pagination and record deduplication, uncertain-submission recovery without blind POST retries, and separate task/answer validity.
- Added body-only exact brand evidence, versioned reanalysis with stable sample denominators, optional real OpenAI-compatible JSON-object analysis, neutral versus unknown semantics, recommendation-evidence-qualified rankings, fact-window checks, and distinct reported/unknown/text-extracted sources.
- Added explicit historical reanalysis with corrected current project aliases; stored actual aliases/policy per analysis without changing frozen batches, raw answers or old analyses. Absence diagnostics use actual analysis inputs; comparisons reject mismatched alias/rule bases.
- Added branded/unbranded separation, platform/region/intent filters and grouping, factual and competitor recommendation metrics, and answer-level numerator/denominator/unknown drilldown.
- Added evidence-backed human-reviewable diagnoses, actionable tasks, immutable manual/model content drafts and provenance, publication records, frozen baseline/retest links, actual control-group comparison and conservative Wilson-bound descriptive uncertainty.
- Added server-only optional bearer access control, safe error messages, output secret redaction, backend/runtime test dependency separation, environment and isolated PostgreSQL Compose configuration, architecture and API contracts.

### Verification

- Real uvicorn + HTTP against an isolated empty SQLite database passed catalog → import → analysis → diagnosis → action → content version → publication → retest → comparison. Reanalysis denominator stability, old-snapshot immutability, unknown/failed samples, branded separation, control samples, archived references and no-key 503 behavior were exercised.
- `python -m pytest tests/test_contract.py -q -p no:seleniumbase`: **22 passed in 14.75s**. The switch disables an unrelated globally installed pytest plugin, not project tests. Includes controlled local HTTP source/model protocol checks, task pagination/state/deduplication/rollback, semantic rank/neutral behavior, Unicode evidence validation, valid-from/inclusive and valid-to/exclusive factual boundaries, generated draft validation and publication, and corrected-alias historical reanalysis with stable denominators, unchanged history and mismatched comparison rejection.
- No live paid model or actual platform account requests were made. Controlled HTTP fixtures verify adapter behavior, not platform integration quality or model factual accuracy.
- PostgreSQL was not runtime-tested: Docker client is installed but its only context points to a missing `docker_engine` named pipe; native postgres/pg_ctl/initdb binaries were unavailable. No system installation, existing user database, or user credentials were used.
- Backend-owned temporary smoke services and SQLite databases were stopped/removed after frontend browser confirmation. Default database was left unseeded for the frontend-owned normal startup.

### GEO frontend and local launcher

- Added a responsive Chinese Next.js / TypeScript six-area workspace with project selection, source mapping, immutable manual answer import, grouped metrics and evidence drilldown, diagnosis review, multi-select task/fact/question/content relationships, publication records and matched retests.
- Added same-origin server-side API proxy using `GEO_API_URL` and `GEO_BACKEND_TOKEN`; no service token is exposed through public client environment variables. Preserved explicit completeness confirmation, unknown-versus-zero semantics and manual review requirements.
- Added explicit current-alias reanalysis checkbox for both rule and model requests, defaulting to frozen batch aliases. Complete version records expose actual input snapshots.
- Added documented PowerShell startup with loopback-only listeners, port preflight, backend health waiting and cleanup of owned backend processes on exit.
- `npm run typecheck && npm run build` passed for the final alias-enabled frontend. Real browser interaction passed project/fact/question creation, multi-select baseline/control selection, manual import, rules analysis, diagnosis review, task/content version/publication and matched second-batch comparison. Unconfirmed complete answers were rejected; unavailable model requests showed an explicit error.
- Browser verified project-switch isolation and a 390px responsive viewport with no horizontal page overflow. Desktop overview, answer evidence, retest and mobile screenshots were reviewed and then removed with the isolated test databases.
- Additional browser alias regression confirmed the checkbox starts unchecked; explicit current-alias reanalysis changed mention false → true in a new version, retained the original text and old false version, and kept the sample denominator at one.
- `scripts/start-local.ps1 -Production` was launched against an isolated empty database: backend/database health ok, proxy projects `[]`, frontend HTTP 200. Ctrl+C exited with code 0 and released both ports. The alias verification service was likewise stopped and its database/sidecars removed.
- A normal unseeded local service was then started without a `DATABASE_URL` test override: `http://127.0.0.1:3000`, backend `127.0.0.1:8000`, default `geo.db`; health/database ok, projects `[]`, frontend HTTP 200. Managed process name: `geo-platform-local`.

### Editorial workspace redesign

- Replaced the card-heavy dashboard with an editorial research layout: warm paper, ink-green navigation, vermilion accents, local Chinese serif headings and an original non-data orbital illustration. No remote font dependency, fabricated chart or seeded product data was introduced.
- Added a refined empty folio and actionable no-sample project state. Existing brand facts, questions, acceptance criteria and content versions appear before editing; native disclosures reveal forms progressively and report their actual expanded/collapsed state.
- Reworked branded/unbranded observations into asymmetric metric strips with genuine sample counts and denominators; retained unknown semantics, four recommendation states and evidence drilldown.
- Added desktop answer-list/original-text columns and reader-first verified quote excerpts with Unicode codepoint ranges and fact references. Full analysis/input JSON remains under an advanced disclosure; raw text and backend contracts are unchanged.
- Unified project-opening scroll/focus behavior with reduced-motion support; evidence-to-task actions explicitly open the task form and focus its title. Existing current-alias reanalysis, completeness confirmation, immutable versions, source configuration and matched retest workflows remain available.
- Final `npm run typecheck && npm run build` passed. A separate SQLite runtime exercised all six areas through actual browser project/fact/question creation, multi-select baseline/control setup, completeness rejection and manual imports, rule analysis, diagnosis acceptance, task/content publication and matched second-batch comparison.
- Visual review passed at CSS viewports 1440, 768 and 390; tablet/mobile scroll width matched the viewport. Long question/form and long-answer evidence views were checked at 390px. Project opener focus/visibility, task disclosure/focus, disclosure labels, 13px tablet sample-note spacing and readable quote/closed advanced-JSON behavior were verified directly.
- Redesign-only processes and database/sidecars were removed. The existing official `geo.db` was preserved, and production was restored via `scripts/start-local.ps1 -Production` under `geo-platform-local`; backend/database health ok, frontend HTTP 200, current project count 0. Reviewed test screenshots are not stored in the repository.
